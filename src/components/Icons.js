import Icon from "react-native-vector-icons/MaterialIcons";
import { TouchableWithoutFeedback } from "react-native";
import React from "react";

export const HomeIcon = () => <Icon name={"home"} size={25} color={"red"} />;
